public class Method {
    public String homework(String input){

        int len = input.length();
        if(len==3)
        return String.valueOf(input.toUpperCase().length());
        return input;
    }
}
